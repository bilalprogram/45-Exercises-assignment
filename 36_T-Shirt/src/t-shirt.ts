// T-Shirt: write a function called make_shirt() that accepts a size and the text of a message that should be printed on the shirt. The function should print a sentence sumarizing the size of the shirt and the message printed on it. 

// Call the function.

function make_shirt(size:string, text:string){
    console.log(`Creating a ${size} shirt with the message: ${text}`);
}
 
make_shirt('Large','Nice T-Shirt');

make_shirt('Medium','Nice T-Shirt');

make_shirt('Small','Nice T-Shirt');

